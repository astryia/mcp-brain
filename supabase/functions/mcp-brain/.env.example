# Copy to .env for local development (supabase functions serve auto-loads this)
BRAIN_SECRET=change-me-to-a-random-secret
# SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are auto-injected by the Edge Runtime
